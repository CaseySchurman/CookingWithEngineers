<!-- Unfollow user button -->
<%= form_for(current_user.active_follows.find_by(followed_id: @user.id), 
             remote: true,
             html: { method: :delete}) do |f| %>
             <br>
    <%= f.submit "Unfollow", class: "button trans2 center buttonBoxBig buttonText" %>
<% end %>